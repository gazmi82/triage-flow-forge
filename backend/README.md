# Backend Scaffold

This folder contains the Go backend foundation for orchestrating Postgres + Redis.

## Run

```bash
cd backend
make run
```

## Health endpoints

- `GET /health`
- `GET /v1/ready`
